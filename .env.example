# Configurações de ambiente

# Ambiente da aplicação (development, production, testing)
FLASK_ENV=development

# Chave secreta para sessões (gere uma nova para produção)
SECRET_KEY=your-secret-key-here

# URL do banco de dados (para produção no Render)
DATABASE_URL=postgresql://user:password@host:port/database

# Configurações alternativas do PostgreSQL (se DATABASE_URL não estiver disponível)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=almoxarifado
DB_USER=postgres
DB_PASSWORD=your-password

# URL do banco de dados para desenvolvimento (SQLite)
DEV_DATABASE_URL=sqlite:///database/app.db

